"""Shared configuration constants for the patient-scheduler app."""

# The public base URL the app is deployed at. Used to build the booking link
# encoded in the QR code, so it must always match wherever this app is
# actually hosted.
BASE_URL = "https://soitirakis.pythonanywhere.com"
